= 2.1 Real-time PDF Processing
include::_attributes.adoc[]

In this section, you will explore the fully automated, event-driven pipeline that was built as part of the prototype. This workflow showcases how we can ingest unstructured documents, like technical manuals in PDF format, in real-time.

The goal is to show how a simple action—uploading a file—can trigger a complex, multi-step data processing workflow without any manual intervention. Importantly, this pipeline supports **multi-user routing**: each workshop participant has their own dedicated S3 bucket and pipeline endpoint, so uploads are automatically routed to the correct user's environment.

== Architecture of the Event-Driven Pipeline

Let's quickly review the architecture of the workflow we are about to see.

[.bordershadow]
image::02/event-driven-pipeline-diagram.png[Architecture diagram of the event-driven PDF ingestion workflow.]

**The flow is as follows:**

1.  A user uploads a PDF file to a **MinIO S3 bucket**.
2.  MinIO immediately sends an event notification to an **Apache Kafka** topic.
3.  A **Knative KafkaSource** consumes the message from Kafka and converts it into a standard CloudEvent.
4.  The CloudEvent is sent to the **Knative Broker**.
5.  A **Knative Trigger** filters for this specific event type and invokes the `kfp-s3-trigger` target.
6.  The `kfp-s3-trigger`, a serverless function, receives the event, determines which user's bucket it came from, and launches a **Kubeflow Pipeline** on that user's Data Science Pipelines Application (DSPA).
7.  The Kubeflow Pipeline runs a component using the **Docling** library to parse the PDF.

Let's trace the event through each step of this process.

'''

== Prerequisite: Import the PDF Processing Pipeline

Before you can trigger a pipeline run, you need to import the PDF processing pipeline into your Data Science Project. This is a one-time setup step.

=== Step 1: Download the Pipeline File from Your Workbench

1.  In the OpenShift AI dashboard, navigate to your project and launch your *Workbench* (named "My Workbench").

2.  Once JupyterLab opens, use the file browser on the left to navigate to:
+
[.console-input]
[source,text]
----
hello-chris-rag-pipeline/lab-content/2.1/
----

3.  Inside this directory, you will find the file `simple-pdf-processing-pipeline.yaml`. *Right-click* on the file and select *Download* from the context menu. This will save the file to your local computer.

4.  You can now close the JupyterLab browser tab.

=== Step 2: Import the Pipeline

1.  In the OpenShift AI dashboard, navigate to the *Pipelines* section within your Data Science Project.

2.  Click the *Import pipeline* button.

3.  In the "Import pipeline" dialog, ensure the *Upload a file* option is selected.

4.  Give your pipeline this exact name:
+
[.console-input]
[source,text]
----
simple-pdf-processing-pipeline
----
+
[IMPORTANT]
====
The name must match exactly. The event-driven trigger service looks up the pipeline by this display name.
====

5.  Click the *Upload* box and select the `simple-pdf-processing-pipeline.yaml` file that you just downloaded.

6.  Click *Import pipeline* to finish.

Once imported, you are ready to trigger the event-driven flow.

'''

== Step 1: Uploading a PDF to MinIO

The entire process begins by uploading a file to an S3 bucket. The system supports two types of buckets:

* **`pdf-inbox`** — the shared demo bucket (routes to the default KFP endpoint)
* **`{user}-pdf-inbox`** — per-user buckets (e.g., `user1-pdf-inbox`, `user2-pdf-inbox`) that automatically route to each user's own DSPA

Try uploading to your per-user bucket (`{user}-pdf-inbox`):

1.  Find the MinIO console route. In the OpenShift Web Console, switch to the *minio* project and navigate to *Networking* -> *Routes*. Click the `minio-console` route URL.
+
Alternatively, you can find the route from the OpenShift Web Terminal:
+
[.console-input]
[source,bash,role="copypaste"]
----
oc get route minio-console -n minio -o jsonpath='{.spec.host}{"\n"}'
----

2.  Log in to the MinIO console using the following credentials:
+
[cols="1,2"]
|===
|Username |`minio`
|Password |`minio123`
|===

3.  In the MinIO browser, navigate to the *Object Browser* section in the left sidebar. Find and click on your per-user bucket: `{user}-pdf-inbox`.

4.  Upload a sample PDF document. You can use any small PDF file from your computer. If you don't have one handy, you can find `example.pdf` in the workbench at:
+
[.console-input]
[source,text]
----
hello-chris-rag-pipeline/scripts/example.pdf
----

// [.bordershadow]
// image::02/minio-upload-pdf.png[Uploading a sample PDF file to a bucket in the MinIO UI.]

As soon as the upload is complete, the MinIO server fires an event. The bucket name is carried along with the event through the entire pipeline, enabling the system to route the processing to the correct user's namespace.

== Step 2: Observing the Event Flow

The `minio-event-bridge` service receives the webhook from MinIO and immediately publishes a CloudEvent message to our Kafka broker. The event includes the bucket name as a CloudEvent extension attribute (`bucketname`), which is critical for multi-user routing downstream.

You can view the event bridge logs to see events flowing through. Open the *OpenShift Web Terminal* (click the *>_* icon in the top-right of the OpenShift Console) and run:

[.console-input]
[source,bash,role="copypaste"]
----
oc logs -n rag-pipeline-workshop -l app=minio-event-bridge --tail=20
----

You will see log entries showing the CloudEvent being received from MinIO and forwarded to the Kafka broker, confirming the event pipeline is active.

// [.bordershadow]
// image::02/kafka-console-consumer-output.png[Output of the Kafka console consumer showing the raw JSON event from the MinIO PDF upload.]

== Step 3: From Kafka to Knative

Now, we will see how Knative Eventing picks up the event and delivers it to the trigger service.

. **Knative Broker & Trigger:** The Kafka Broker receives the CloudEvent (including the `bucketname` extension) and delivers it to any matching subscribers. Our `kfp-s3-trigger` service is subscribed via a Trigger. You can verify the Trigger is ready and pointing to the correct service:
+
[.console-input]
[source,bash,role="copypaste"]
----
oc get trigger minio-pdf-event-trigger -n rag-pipeline-workshop
----
+
The output should show the Trigger with `READY = True`, subscribed to the `kafka-broker` with its subscriber destination set to the `kfp-s3-trigger` Knative Service.

== Step 4: The Serverless Function (`kfp-s3-trigger`)

The final step in the eventing chain is our custom serverless function. When the Trigger fires, the Knative Broker delivers the CloudEvent to a running instance of our `kfp-s3-trigger` pod.

This is where the **multi-user routing** happens. The `kfp-s3-trigger` service:

1.  Extracts the bucket name from the CloudEvent body (and/or the `Ce-Bucketname` header).
2.  Determines whether it's a per-user bucket (e.g., `user1-pdf-inbox`) or the shared demo bucket (`pdf-inbox`).
3.  Routes to the correct DSPA endpoint:
    * Per-user bucket `user1-pdf-inbox` -> `ds-pipeline-dspa.user1.svc.cluster.local:8443`
    * Shared bucket `pdf-inbox` -> the default `KFP_ENDPOINT` (fallback)

View the logs of this pod:

[.console-input]
[source,bash,role="copypaste"]
----
oc logs -n rag-pipeline-workshop -l serving.knative.dev/service=kfp-s3-trigger -c user-container --tail=30
----

You will see log entries confirming:

* A POST request was received with the CloudEvent payload.
* The bucket name and object key were extracted from the event.
* The correct KFP endpoint was resolved (e.g., "Bucket '{user}-pdf-inbox' -> user '{user}' -> KFP endpoint: ...").
* The KFP client was initialized for the resolved endpoint.
* The pipeline was found and a run was started with the S3 object key as a parameter.

// [.bordershadow]
// image::02/s3-handler-logs.png[Logs from the kfp-s3-trigger pod showing multi-user routing and the successful triggering of a KFP run.]

== Step 5: The Kubeflow Pipeline Run

The event has now successfully traversed the entire serverless infrastructure and launched a data processing workflow in the correct user's namespace.

1.  Navigate back to the **OpenShift AI Dashboard**.
2.  Inside your Data Science Project (`{user}`), click on **Pipelines** -> **Runs**.
3.  A new run will be visible at the top of the list in the **S3 Triggered PDF Runs** experiment.

Because the `kfp-s3-trigger` service dynamically routes each event to the correct DSPA endpoint, the pipeline run appears in the namespace of the user whose bucket was used. For example:

* A PDF uploaded to `user1-pdf-inbox` triggers a pipeline run in the `user1` namespace.
* A PDF uploaded to `user2-pdf-inbox` triggers a pipeline run in the `user2` namespace.

Clicking on the run reveals the graph of the pipeline that was executed. We can see the `download-pdf-from-s3` and `process-pdf-with-docling` components, and by clicking on them, we can inspect their inputs, outputs, and logs.

[NOTE]
====
The first pipeline run may take a few minutes as the Docling container image is large. Subsequent runs will be faster since the image is cached on each node.
====

// [.bordershadow]
// image::02/kfp-run-graph-complete.png[The graph of the completed PDF processing pipeline run in the Kubeflow Pipelines UI.]

This completes the walkthrough. You have now seen an end-to-end, automated, and event-driven workflow for processing unstructured data on OpenShift AI — with automatic multi-user routing that allows each workshop participant to trigger their own pipeline runs independently.
